# Acknowledgements

This adapter packages and renders with **Apache ECharts**, and would not exist without it.

- **Apache ECharts** — https://echarts.apache.org · © The Apache Software Foundation,
  licensed under the **Apache License 2.0**. The bundled `echarts.min.js` is the unmodified
  upstream library; see `LICENSE` and `NOTICE`.
- **Logo** — the Apache ECharts logo (`logo.png`) is a trademark of The Apache Software
  Foundation, sourced from the official site (echarts.apache.org). Used here to identify the
  upstream engine; not an endorsement by the ASF.

The MyBI **adapter** code (the per-library draw built against the MyBI chart host SDK) is
licensed MIT. ECharts and the ASF are independent of MyBI.
